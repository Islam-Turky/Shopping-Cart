<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - SampleApplication</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/SampleApplication.styles.css" asp-append-version="true" />
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-dark border-bottom box-shadow mb-3">
            <div class="container-fluid">
                <a class="navbar-brand" asp-area="" asp-controller="Store" asp-action="Index" style="font-weight:900; color: white; font-style: italic">Store</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                @* <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between align-content-center" style="direction: rtl">
                    <ul class="navbar-nav flex-grow-1 gap-5">
                        <li class="nav-item">
                            <a class="nav-link text-white" asp-area="" asp-controller="User" asp-action="Login" style="font-size: 20px; font-weight: 600" id="log">Login</a>
                        </li>
                        @if (TempData["name"] != "not exist")
                        {
                            <li class="nav-item">
                                <a class="nav-link text-white bg-dark" asp-controller="User" asp-action="Products" id="avatar" style="font-size: 20px; font-weight: 600"></a>
                            </li>
                        }
                    </ul>
                </div> *@
                <partial name="_LoginPartial" />
            </div>
        </nav>
    </header>
    <div class="container">
        <main role="main">
            @* <div class="w-75"> *@
                @RenderBody()
            @* </div> *@
        </main>
    </div>

    <footer class="border-top footer text-muted bg-dark">
        <div class="container text-center">
            &copy; 2024 - Store - <a asp-area="" asp-controller="Store" asp-action="Privacy">Privacy</a>
        </div>
    </footer>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
   @*  <script>
        var uname = window.localStorage.getItem("UserName");
        var log = document.getElementById("log");
        if(uname != null)
        {
            log.innerHTML = "Logout";
            log.onclick = () => {
                window.localStorage.setItem("UserName", "");
                // location.reload();
            }
            document.getElementById("avatar").innerHTML = uname;
        }
        if(uname == "")
        {
            log.innerHTML = "Login";
        }
    </script> *@
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
